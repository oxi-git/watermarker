{
  "identifier": "default",
  "description": "Permessi default: salvataggio file via dialog nativo",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "fs:allow-write-file"
  ]
}
